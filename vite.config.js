import { defineConfig } from 'vite'

export default defineConfig({
  // No specific configuration needed for this simple project
  // If you were using React, you'd add the react plugin here:
  // plugins: [react()],
})
